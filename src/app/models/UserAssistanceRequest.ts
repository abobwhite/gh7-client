import {ASSISTANCE_CAPABILITY} from './ASSISTANCE_CAPABILITY';

export class UserAssistanceRequest {
  id: string;
  requestingUserId: string;
  requestedCapability: ASSISTANCE_CAPABILITY;

  // createdAt
  // rejectedVolunteerIds
}
